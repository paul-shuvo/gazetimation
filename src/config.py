
# If not sure set camera_index is to -1, it automatically
# tries to find the device. The library iterates through
# index 0 to 9 to find the index, if it doesn't it'll
# raise an error.
camera_index = 0